#include "main.h"

/**
 * main - check the code.
 * Return: always 0.
 */
int main(void)
{
	char *str;
	str = "Ido not fear computers, i fear the lack of them - isaa asimov";
	print_rev(str);
	return (0);
}
